//// We use this to say that name must be a string 
// and age must be a number before using them
export interface childCompProps {
  name:string;
  age:number;
}

export interface secondChildProps{
  name:string;
  lastName: string;
  isStundent:string;
  isHardworker:string;
}